import { prependItem, excludeOn } from 'piral-core';
export function openNotification(ctx, dialog) {
    ctx.dispatch((state) => (Object.assign(Object.assign({}, state), { notifications: prependItem(state.notifications, dialog) })));
}
export function closeNotification(ctx, dialog) {
    ctx.dispatch((state) => (Object.assign(Object.assign({}, state), { notifications: excludeOn(state.notifications, (notification) => notification.id === dialog.id) })));
}
//# sourceMappingURL=actions.js.map