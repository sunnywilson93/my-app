import * as React from 'react';
import { PiralLoadingIndicator, PiralError } from 'piral-core';
import { useFeed } from './useFeed';
export function withFeed(Component, options) {
    const FeedView = (props) => {
        const [loaded, data, error] = useFeed(options);
        if (!loaded) {
            return React.createElement(PiralLoadingIndicator, null);
        }
        else if (data) {
            return React.createElement(Component, Object.assign({}, props, { data: data }));
        }
        else {
            return React.createElement(PiralError, { type: "feed", error: error });
        }
    };
    FeedView.displayName = `FeedView_${options.id}`;
    return FeedView;
}
//# sourceMappingURL=withFeed.js.map