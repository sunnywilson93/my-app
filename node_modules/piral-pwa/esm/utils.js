export function useNotification(reg, notify) {
    return (reg &&
        Notification.requestPermission().then((permissions) => {
            if (permissions === 'granted') {
                return notify();
            }
        }));
}
//# sourceMappingURL=utils.js.map